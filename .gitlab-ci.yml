stages:
  - infrastructure
  - kubernetes
  - platform
  - gitops

# Настройки для всех джобов
default:
  tags:
    - shell
  before_script:
    - echo ">>> Подготовка рабочего окружения..."
    - cp terraform/.terraformrc ~/.terraformrc || true
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_ed25519
    - chmod 600 ~/.ssh/id_ed25519
    - eval $(ssh-agent -s)
    - ssh-add ~/.ssh/id_ed25519
    - export ANSIBLE_HOST_KEY_CHECKING=False
    - export KUBECONFIG=~/.kube/config

# ==========================================
# ЭТАП 1: Инфраструктура (Raw Terraform)
# ==========================================
deploy_infra:
  stage: infrastructure
  script:
    - echo ">>> Развертывание узлов K8s (Terraform)..."
    - cd terraform/cluster-nodes
    - terraform init
    - terraform apply -state=$HOME/k8s-cluster.tfstate -auto-approve
  # Сохраняем сгенерированный инвентарь для Ansible
  artifacts:
    paths:
      - ansible/inventory/generated_hosts.ini
    expire_in: 1 hour

# ==========================================
# ЭТАП 2: Кубернетес (Raw Ansible + kubectl)
# ==========================================
deploy_k8s:
  stage: kubernetes
  script:
    - echo ">>> Настройка ОС и Bootstrap Kubernetes..."
    - sleep 30
    - ansible-playbook -i ansible/inventory/generated_hosts.ini ansible/playbooks/01_prepare.yml
    - ansible-playbook -i ansible/inventory/generated_hosts.ini ansible/playbooks/02_install_k8s.yml
    - ansible-playbook -i ansible/inventory/generated_hosts.ini ansible/playbooks/02_init_master.yml
    - ansible-playbook -i ansible/inventory/generated_hosts.ini ansible/playbooks/02_join_workers.yml
    - ansible-playbook -i ansible/inventory/generated_hosts.ini ansible/playbooks/01_containerd_mirrors.yml
    
    - echo ">>> Ожидание готовности кластера..."
    - kubectl wait --for=condition=Ready nodes --all --timeout=120s
    
    - echo ">>> Настройка MetalLB (защита от Race Condition)..."
    - kubectl apply -f kubernetes/core/metallb-native.yaml
    - kubectl wait --namespace metallb-system --for=condition=ready pod -l component=controller --timeout=300s
    - kubectl wait --namespace metallb-system --for=condition=ready pod -l component=speaker --timeout=300s
    - sleep 15
    - kubectl apply -f kubernetes/core/metallb-config.yaml
  needs: ["deploy_infra"]

# ==========================================
# ЭТАП 3: Хранилище и Мониторинг (100% Локально)
# ==========================================
deploy_platform:
  stage: platform
  script:
    - echo ">>> Расшифровка секретов (Локальный SOPS)..."
    - chmod +x bin/sops
    - ./bin/sops -d kubernetes/core/truenas-csi/zfs-iscsi-secrets.yaml > truenas-secrets.yaml
    - ./bin/sops -d kubernetes/core/alertmanager-values-secrets.yaml > alertmanager-secrets.yaml

    - echo ">>> Развертывание Democratic CSI (Локальный чарт)..."
    - helm upgrade --install truenas-iscsi kubernetes/core/democratic-csi --namespace democratic-csi --create-namespace -f kubernetes/core/truenas-csi/zfs-iscsi-base.yaml -f kubernetes/core/truenas-csi/zfs-iscsi-prod.yaml -f truenas-secrets.yaml
    
    - echo ">>> Установка стека мониторинга (Локальный .tgz архив)..."
    - helm upgrade --install kube-prometheus-stack kubernetes/charts/kube-prometheus-stack-*.tgz --namespace monitoring --create-namespace -f alertmanager-secrets.yaml -f kubernetes/core/grafana-dashboards.yaml --set grafana.service.type=LoadBalancer --set prometheus.service.type=LoadBalancer --set alertmanager.service.type=LoadBalancer --set kubeEtcd.enabled=false --set kubeControllerManager.enabled=false --set kubeScheduler.enabled=false --set kubeProxy.enabled=false
    
    - echo ">>> Установка стека логирования (Локальный .tgz архив)..."
    - helm upgrade --install loki kubernetes/charts/loki-stack-*.tgz --namespace monitoring -f kubernetes/core/loki-values.yaml
    
    - echo ">>> Применение кастомных правил и дашбордов..."
    - kubectl apply -f kubernetes/core/loki-datasource.yaml
    - kubectl apply -f kubernetes/core/baremetal-monitors.yaml
    - kubectl apply -f kubernetes/core/hedgehog-rules.yaml
    - kubectl apply -f kubernetes/core/network-alerts.yaml
    
    - echo ">>> Очистка расшифрованных секретов из памяти Runner..."
    - rm -f truenas-secrets.yaml alertmanager-secrets.yaml
  needs: ["deploy_k8s"]

# ==========================================
# ЭТАП 4: ArgoCD (100% Локально)
# ==========================================
deploy_gitops:
  stage: gitops
  script:
    - export KUBECONFIG=$HOME/.kube/config
    - echo ">>> Создание неймспейса и установка ArgoCD..."
    - kubectl create namespace argocd || true
    - kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml --server-side --force-conflicts
    - echo ">>> Фикс: замена капризного образа Redis с ECR на Docker Hub..."
    - kubectl set image deployment/argocd-redis redis=redis:7-alpine -n argocd
    - echo ">>> Ожидание запуска подов ArgoCD..."
    # Тот самый увеличенный таймаут на 900s
    - kubectl wait --for=condition=Ready pods --all -n argocd --timeout=900s
    - echo ">>> Передача управления кластеру в ArgoCD (GitOps)..."
    - kubectl apply -f kubernetes/argocd-apps.yaml
