## 🛠 Управление проектом (Makefile)

Вся инфраструктура и бэкапы управляются через центральный `Makefile`. Перед использованием команд бэкапа убедитесь, что у вас создан файл `.restic_env` в корне проекта (он добавлен в `.gitignore` для безопасности).

### 🚀 Развертывание кластера
* `make all` — Полный цикл: поднимает виртуалки (Terraform) -> генерирует секреты из Vault -> настраивает ОС (Ansible) -> ставит K8s -> разворачивает сеть и балансировщик -> подключает iSCSI хранилище TrueNAS -> деплоит приложения.
* `make down` — Полное уничтожение стенда: корректно удаляет приложения и диски PVC, после чего уничтожает виртуалки в Terraform.
* `make apps` — Применить только манифесты приложений из `kubernetes/apps/`.

### 🔐 Секреты (Ansible Vault)
Пароли и API-ключи хранятся в зашифрованном файле `ansible/vault.yml`. 
* `make generate-secrets` — Вручную сгенерировать `zfs-iscsi-secrets.yaml` для дебага (спрашивает пароль от Vault). В основном цикле `make all` этот файл создается и удаляется автоматически.

### 📦 Бэкапы кода (Restic + S3 MinIO)
Резервное копирование кода инфраструктуры производится на TrueNAS (S3).
* `make backup` — Запустить бэкап текущего состояния проекта (исключая `.git` и `venv`). Потребует пароль от ОС (для установки restic) и пароль от Vault (для получения ключей).
* `make backup-list` — Показать все точки восстановления (снимки) в S3.
* `make backup-ls` — Посмотреть структуру файлов внутри последнего бэкапа.
* `make backup-check` — Проверить целостность зашифрованных данных в S3.
* `make restore` — Тестовое восстановление последнего бэкапа в папку `/tmp/restore`.
