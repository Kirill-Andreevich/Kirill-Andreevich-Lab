# 🏗 Kirill's Zen 5 Infrastructure Lab

Профессиональный стенд для отработки IaC-подходов и автоматизированного развертывания AI-сервисов.

## 🎯 Философия
**"One Button to Rule Them All"**: Вся инфраструктура от гипервизора до DeepSeek-R1 должна разворачиваться одной командой `make all`. Никакого ручного вмешательства.

## 💻 Hardware Topology
*   **Compute Node (.22):** AMD Ryzen 9 9950X3D | 128GB RAM | NVIDIA RTX 4090.
*   **Workstation (.234):** AMD Ryzen 7 9800X3D | AMD RX 9070 XT.
*   **Storage (.176):** TrueNAS ZFS.

## 🛠 Tech Stack
*   **Infrastructure:** Terraform (Libvirt / QEMU).
*   **Configuration:** Ansible (Role-based).
*   **OS:** Ubuntu 24.04 LTS (Noble Numbat).
*   **AI Stack:** Ollama (DeepSeek-R1 / Qwen 2.5 Coder).

## 🔧 Ключевые доработки (Technical R&D)

В процессе настройки **Ubuntu 24.04** на базе **L2 Bridge** были решены следующие критические задачи:

### 1. Стабильность загрузки (Storage Tuning)
*   **Проблема:** Стандартные Cloud-образы (2GB) «висли» на этапе инициализации `systemd` из-за нехватки места для логов и расширения ФС.
*   **Решение:** В Terraform реализовано принудительное расширение системного диска до **20GB** (`size = 21474836480`).

### 2. Сетевая связность (Bridge Networking)
*   **Проблема:** Хост (Arch Linux) блокировал DHCP-трафик для ВМ через `br0`.
*   **Решение:** Настройка `sysctl` (`net.bridge.bridge-nf-call-iptables = 0`) и загрузка модуля `br_netfilter` для прозрачного прохождения L2-трафика.
*   **Zero-Conf:** Использование `wait_for_lease = true` в Terraform для динамического ожидания IP от внешнего DHCP.

### 3. Автоматизация Inventory (Ansible Integration)
*   **Проблема:** Склеивание IPv4 и IPv6 адресов в одну строку при генерации инвентаря.
*   **Решение:** Использование функции `${element(vm.network_interface.0.addresses, 0)}` для фильтрации чистого IPv4.
*   **Телеметрия:** Внедрение **QEMU Guest Agent** через Cloud-Init для передачи IP-адреса гостевой ОС обратно в гипервизор.

## 📝 Текущий статус
* [done] Настройка прозрачного L2 Bridge (br0) для Zero-Conf связности.
* [done] Миграция на архитектуру Terraform v2 (for_each).
* [done] Автогенерация динамического инвентаря Ansible.
* [in progress] Деплой GPU-воркеров для инференса DeepSeek-R1.

---
*Created with 🧠 by Kirill's Homelab Automation*
